@{
    ViewData["Title"] = Html.T("Account.ForgotPasswordConfirmationTitle", "Şifre Sıfırlama Kodu Gönderildi").ToString();
    Layout = "_Layout";
}

<div class="row justify-content-center">
    <div class="col-md-5">
        <div class="card shadow">
            <div class="card-body p-4 text-center">
                <i class="bi bi-envelope-check text-success" style="font-size: 4rem;"></i>

                <h3 class="mt-3">@Html.T("Account.EmailSent", "E-posta Gönderildi")</h3>

                <p class="text-muted">
                    @(TempData["SuccessMessage"] ?? Html.T("Account.ForgotPasswordConfirmation", "Eğer bu e-posta adresi sistemde kayıtlıysa, şifre sıfırlama bağlantısı gönderildi.").ToString())
                </p>

                @if (TempData["ResetToken"] != null)
                {
                    <div class="alert alert-info mt-3">
                        <small class="text-muted d-block mb-2">
                            <i class="bi bi-info-circle me-1"></i>
                            Development Modu - Token bilgisi:
                        </small>
                        <div class="bg-light p-2 rounded">
                            <strong>Token:</strong> @TempData["ResetToken"]
                        </div>
                        <div class="mt-2">
                            <a asp-action="ResetPassword" asp-route-token="@TempData["ResetToken"]" asp-route-email="@TempData["Email"]" class="btn btn-primary btn-sm">
                                <i class="bi bi-arrow-right me-1"></i>@Html.T("Account.GoToResetPassword", "Şifre Sıfırlamaya Git")
                            </a>
                        </div>
                    </div>
                }

                <div class="mt-4">
                    <a asp-action="Login" class="btn btn-outline-primary">
                        <i class="bi bi-arrow-left me-1"></i>@Html.T("Account.BackToLogin", "Giriş Sayfasına Dön")
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>
